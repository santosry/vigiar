% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rj.R
\name{vigiar_rj_completude_tabela}
\alias{vigiar_rj_completude_tabela}
\title{Check RJ completeness using the table's expected panel grain}
\usage{
vigiar_rj_completude_tabela(
  dados,
  tabela = NULL,
  require_complete = FALSE,
  anos_esperados = NULL,
  meses_esperados = NULL,
  periodo_inicio = NULL,
  periodo_fim = NULL,
  inferir_periodos = TRUE
)
}
\arguments{
\item{dados}{A data frame with municipality codes.}

\item{tabela}{Optional table name. Defaults to the `vigiar_tabela` attribute.}

\item{require_complete}{If \code{TRUE}, incomplete coverage, invalid temporal
values, or possible truncation is an error.}

\item{anos_esperados}{Optional integer vector defining the expected years.}

\item{meses_esperados}{Optional integer vector defining expected months.}

\item{periodo_inicio}{Optional first expected period as a Date or a
`YYYY-MM`/`YYYY-MM-DD` string.}

\item{periodo_fim}{Optional last expected period in the same format as
`periodo_inicio`.}

\item{inferir_periodos}{If \code{TRUE}, fill periods between the minimum and
maximum observed values when no expected domain is supplied.}
}
\value{
A tibble with RJ coverage metrics at the expected table grain.
}
\description{
This function distinguishes "data were downloaded" from "the expected RJ
panel is complete". For `df_mensal`, completeness is assessed by
municipality x year x month. For `df_anual`, completeness is assessed by
municipality x year. For `df_dias` and `df_dias_conama`, completeness is
assessed by municipality x year x month when month is available, otherwise
by municipality x year.
}
